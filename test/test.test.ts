import { describe, expect, it } from 'vitest';

describe('CreateUser', () => {
  const sut = { test: 'test' };

  it('should return a valid payload', async () => {
    expect(sut).toEqual(sut);
  });
});
